<% flash.each do |key, value| %>
    <div class="alert alert-dismissible alert-<%= "#{flash_key(key)}" %>">
      <button type="button" class="close" data-dismiss="alert">&times;</button>
      <%= value %>
    </div>
<% end %>